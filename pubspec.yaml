name: arion_code_challenge
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: ^6.0.0
  dio: ^4.0.4
  get_it: ^7.2.0
  equatable: ^2.0.3
  flutter_lints: ^1.0.0
  shared_preferences: ^2.0.11
  cached_network_image: ^3.2.0
flutter:

  uses-material-design: true